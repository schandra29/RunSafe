{
  "name": "runsafe",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "runsafe": "./bin/runsafe"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "commander": "^11.0.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {},
  "scripts": {
    "dev": "bun src/index.ts",
    "test": "bun test",
    "build": "bun build src/index.ts --outdir dist",
    "test:debug": "bun --inspect-brk test"
  }
}
